class Solution {
    public int titleToNumber(String columnTitle) {
        int column = 0;
        for(char c: columnTitle.toCharArray()){
            column *=26;
            column += (c -'A'+1);
        }
        return column;
    }
}